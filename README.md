I created this repo to learn about the underlying architecture of json web tokens

TODO: 

1. Issue a JWT with random payload
2. Verify that issued JWT to get back the random payload

3. Do the above first for a shared secret, then use asymmetric signing and do the above with both public and private keys